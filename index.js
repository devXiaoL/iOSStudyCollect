import React, { Component } from 'react';
import {AppRegistry, View, Text, StyleSheet, TouchableOpacity} from 'react-native';
import App from './src/App'

AppRegistry.registerComponent('RNPage', () => App);
